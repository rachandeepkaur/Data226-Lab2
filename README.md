# Data226 Lab 2 repository

**Deploy Airflow against the single project root:** [`weather-analytics/`](weather-analytics/README.md) — it contains Lab 1 + Lab 2 DAGs, `src/`, `sql/`, and `dbt/`.

Do not point `dags_folder` at multiple copies of the same DAGs.
